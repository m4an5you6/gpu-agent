#!/usr/bin/env python3
"""
GPUCLOUD Agent CLI launcher.

This wrapper should behave like the installed `gpucloud` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from gpucloud_cli.main import main
    main()
